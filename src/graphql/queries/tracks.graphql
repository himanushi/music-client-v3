query Tracks(
  $cursor: CursorInputObject
  $sort: TracksSortInputObject
  $conditions: TracksConditionsInputObject
) {
  items: tracks(cursor: $cursor, sort: $sort, conditions: $conditions) {
    ...TrackFields
  }
}
